@model Application.DTOs.DiscountCodes.CreateDiscountCodeDto
@{
    ViewData["Title"] = "ایجاد کد تخفیف";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
<div class="card-dashboard p-4">
    <h2>ایجاد کد تخفیف جدید</h2>
    <form method="post">
        @Html.AntiForgeryToken()
        <div class="mb-3">
            <label>کد</label>
            <input asp-for="Code" class="form-control bg-dark text-white" required />
        </div>
        <div class="mb-3">
            <label>نوع تخفیف</label>
            <select asp-for="Type" class="form-select bg-dark text-white" asp-items="Html.GetEnumSelectList<Domain.Enums.DiscountType>()"></select>
        </div>
        <div class="mb-3">
            <label>مقدار</label>
            <input asp-for="Value" class="form-control bg-dark text-white" type="number" step="0.01" required />
        </div>
        <div class="mb-3">
            <label>تاریخ شروع (اختیاری)</label>
            <input asp-for="StartDate" class="form-control bg-dark text-white" type="date" />
        </div>
        <div class="mb-3">
            <label>تاریخ پایان (اختیاری)</label>
            <input asp-for="EndDate" class="form-control bg-dark text-white" type="date" />
        </div>
        <div class="mb-3">
            <label>حداکثر تعداد استفاده</label>
            <input asp-for="UsageLimit" class="form-control bg-dark text-white" type="number" min="1" value="1" />
        </div>
        <button type="submit" class="btn btn-gold">ذخیره</button>
        <a asp-action="Index" class="btn btn-secondary">بازگشت</a>
    </form>
</div>